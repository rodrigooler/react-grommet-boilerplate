'use strict';

var _eslintParallel = require('eslint-parallel');

var _eslintParallel2 = _interopRequireDefault(_eslintParallel);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

process.on('message', function (assets) {
  new _eslintParallel2.default({
    cache: true,
    cwd: process.cwd()
  }).execute([assets]).then(function (result) {
    var failed = result.errorCount || result.warningCount;
    if (failed) {
      process.exit(1);
    } else {
      process.exit(0);
    }
  }, function (err) {
    console.log(err);
    process.exit(1);
  });
}); /**
    * NPM dependencies
    **/